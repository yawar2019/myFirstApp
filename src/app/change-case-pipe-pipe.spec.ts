import { ChangeCasePipePipe } from './change-case-pipe-pipe';

describe('ChangeCasePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeCasePipePipe();
    expect(pipe).toBeTruthy();
  });
});
